import styled from 'styled-components';

export const Container = styled.header`
  display: block;
  overflow: hidden;
  width: 100%;
  height: 64px;

  .left {
    float: left;
  }
  .right {
    float: right;
  }
`;
